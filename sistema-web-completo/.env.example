# Conexión a PostgreSQL (coincide con docker-compose.yml de la raíz)
PGHOST=localhost
PGPORT=5432
PGUSER=olimpiadas
PGPASSWORD=olimpiadas123
PGDATABASE=olimpiadas

# Puerto del servicio
PORT=4000

# Secreto JWT (compartido con el servicio de login)
JWT_SECRET=olimpiadas_super_secreto_demo
